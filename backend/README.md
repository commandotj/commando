# Backend (Go module)

Core: `github.com/systembug/commando` — pure Go, no `package.json`.

```text
backend/
  go.mod
  cmd/commando/
  internal/
```

```bash
pnpm cli            # root → make cli
pnpm test:go        # root → make test-go
```
